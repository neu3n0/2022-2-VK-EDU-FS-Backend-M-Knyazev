from django.urls import path
from users.views import create_user, show_users

urlpatterns = [
    # path('create/', create_user, name='create_user'),
    # path('show_users/', show_users, name='show_users'),
]